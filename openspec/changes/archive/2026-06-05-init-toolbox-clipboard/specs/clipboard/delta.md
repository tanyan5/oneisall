# Delta: clipboard (init)

## ADDED

- Clipboard history persistence (SQLite, max 200 items).
- Capture for text, HTML, image, and file list types.
- List UI with time, detail, delete; keyword search; copy back to OS clipboard.
